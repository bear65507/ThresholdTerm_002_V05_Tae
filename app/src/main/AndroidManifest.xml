<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:name=".FocusStudyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ThresholdTerm_002"
        tools:targetApi="31">
        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="${kakaoApiKey}" />

        <meta-data
            android:name="com.kakao.vectormap.APP_KEY"
            android:value="${kakaoApiKey}" />

        <activity
            android:name=".ui.timer.TimerActivity"
            android:exported="false"
            android:label="집중 타이머" />

        <activity
            android:name=".CameraActivity"
            android:exported="false"
            android:label="카메라 분석 타이머" />

        <activity
            android:name=".ml.MediaPipeActivity"
            android:exported="false"
            android:label="AI 실시간 자세분석" />

        <activity
            android:name=".ui.stats.StatsActivity"
            android:exported="false"
            android:label="공부 시간 집계" />

        <activity
            android:name=".ui.library.LibraryActivity"
            android:exported="false"
            android:label="도서관 검색" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
